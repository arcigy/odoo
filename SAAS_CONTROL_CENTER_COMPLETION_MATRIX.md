# SaaS Control Center completion matrix

Authoritative specification: `pasted-text-1.txt` supplied in the Codex task.

This matrix distinguishes implemented contracts from live evidence. A seeded metric without a producing source is not considered complete.

| Area | Status | Current evidence | Remaining proof or action |
|---|---|---|---|
| Raw telemetry outside Odoo | partial | Odoo addon stores bounded current values, timeseries and domain aggregates only; no raw request, trace or product-event payload model exists. | Prove approved log, trace and product analytics backends plus their retention. |
| Develop/Main isolation | deployed, source data pending | `saas.environment` only permits `develop` and `main`; current unique key contains metric, environment and scope; history/aggregate/operational keys require environment prefix. Live ORM returns both columns on all dashboards and a separate conservative freshness/completeness summary for each environment. | Send real values from both isolated environments and reconcile source rows. |
| Metric definition contract | proven in local Odoo 19 | 191 definitions across 24 dashboards; required formula, dimensions, owner, thresholds, freshness, retention, numerator/denominator labels and runbook; safe internal drill-down fallback. Registry tests pass. | Owner/runbook review for each production metric. External observability drill-down links remain source-specific. |
| Mandatory global filters | structurally proven | Normal relation fields and cockpit filters exist for period/comparison, service, region, release, tenant, plan, tenant size, feature, endpoint, job, integration, country, acquisition, browser, OS, device, severity and currency. Browser smoke loads the cockpit and dashboard selector. | Load representative dimension data and confirm large-tenant selector/search behavior. |
| 24 dashboard catalogue | deployed, sources partial | All 23 required dashboards plus optional AI/LLM exist. Cockpit renders one selected dashboard with KPI cards, trends, previous-period comparison, Develop/Main columns, sample size, thresholds, releases, alerts, runbook and history links. The deployed asset contains the paired freshness UI and the live registry exposes all 24 dashboards; current live completeness is honestly 0% because no source rows have been ingested. | Most dashboards still need producing sources and source reconciliation. A signed-in browser smoke of the deployed cockpit remains pending. |
| Recommended base/current/history models | proven in local Odoo 19 | All named base models, `saas.metric.current` and `saas.metric.timeseries` exist with idempotency and bounded validation. Module installation, registry and database-constraint tests pass. | Production-scale query and retention evidence remains required. |
| Domain aggregates | proven locally | All 11 named aggregate models exist with list/pivot/graph actions and an allowlisted idempotent batch ingest. | Wire actual hourly/daily producers and reconcile totals against source systems. |
| Operational records | proven locally | Alerts, incidents, postmortem actions, backups, restore tests, load tests, sync runs and releases exist. Backup/restore/load/data-quality ingest is retry-safe. | Feed real backup, restore, load and data-quality results. |
| Alert workflow | proven in local Odoo 19 | Metric warning/critical status deduplicates by environment/scope; healthy resolves it. Failed backup/restore and invalid data-quality runs open P1; P0/P1 create incident/activity; closure requires root cause and postmortem action. Registry tests pass. | Complete activity/mail smoke and assign production owners and notification channels. |
| Role separation | registry and live ACL tested | Eight specified groups exist. Bot ACL is read-only on all 27 allowed models and cannot create/write/delete directly; negative tests cover finance/security and engineering/cost boundaries. A live read-only access audit confirmed no bot create/write/delete rights on contacts, CRM leads, incidents or integration records. | Inspect menus/API interactively as every human role. Create the bot API key only when an isolated source is ready for an end-to-end sync. |
| Retention | decision required | Per-metric retention exists and `retention_preview` is non-destructive. | User approval is required before any deletion cron: approve policy, backup evidence, dry run, verification and rollback. |
| Arcigy technical source | partial, live URLs confirmed | Isolated `saasOdooMetricSync.ts` reads `/metrics` and `/ready`, calculates eligible HTTP count/success/5xx/429/in-flight/RPS/p50/p95/p99, worker uptime, PostgreSQL readiness latency and privacy-safe browser project-open/app-data-load/runtime metrics, then posts both environments through Odoo JSON-2. Live CapRover inventory confirms Develop `https://arcigy-kitchen-develop.178.104.175.242.sslip.io`, Main `https://app.arcigy.cloud`, and Odoo `https://geotherm-odoo.178.104.175.242.sslip.io`. The collector now rejects HTML/empty/unrecognized telemetry instead of emitting misleading zeros. | Develop `/metrics` currently returns SPA HTML; Main `/ready` and `/metrics` return SPA HTML. Expose authenticated Prometheus telemetry and valid readiness for both, configure secrets, then verify two no-write scrapes and reconciliation before scheduling. |
| DB/cache/queue/dependency sources | adapter ready, producers missing | Definitions, current fields and aggregate targets exist. The standalone Prometheus adapter queries Develop/Main independently, enforces an explicit environment marker on shared backends, accepts exactly one aggregate sample and has a no-write dry-run mode. Four adapter regression tests pass. | Deploy/verify the actual PostgreSQL/pool, queue, cache and provider exporters, replace example PromQL with proven queries, then reconcile two dry runs. Do not synthesize values in Odoo. |
| Product-event pipeline | missing | Product metric definitions and aggregate targets exist. | Implement the specified event contract and approved analytics aggregation without colliding with the active kitchen_app task. |
| Revenue/billing/CRM/support/marketing sources | missing | Definitions and Odoo targets exist. | Add signed, idempotent source integrations and reconciliation from authoritative systems. |
| Security/privacy/CI/CD sources | missing | Definitions, access boundaries and security aggregate exist. | Connect scanners, audit source, CI and privacy registers without sending secrets or raw private payloads. |
| Backup/restore live evidence | partial | Models and safe ingest exist. The current Arcigy audit records a passed synthetic PostgreSQL 16.14 logical restore with checksum, 4 migrations, 24 tables, 32 rows, pricing/BOM/assets, zero tenant leak, RPO 0 and RTO 1.694 seconds. | Send this evidence to Odoo after installation, then add automated encrypted off-host backups, WAL/PITR as required and an approved real-backup restore drill. |
| Load and failure evidence | missing | Load-test model and safe ingest exist. | Run only in an approved isolated environment with synthetic data and stated thresholds. |
| Odoo 19 install/tests/browser | deployed, signed-in browser pending | The addon is installed live together with `geotherm_chatbot` and `geotherm_drive`. Live ORM proves 24 dashboards, 191 definitions, paired Develop/Main payloads and the 1.5x freshness boundary; the deployed image contains the paired freshness asset and `/web/login` returns HTTP 200. Local registry/browser tests previously passed. | Repeat the visual and console smoke in a signed-in live Odoo session. |
| Canonical/deploy reconciliation | deployed | The canonical repo contains all three additive addons plus guarded deployment wiring. Commit `ea9c335` was pushed to `main`; GitHub Actions deploy `29514290084` succeeded and live runtime is healthy on `img-captain-geotherm-odoo:9` with one of one replicas. | Preserve the current image as rollback evidence before any later module/data migration. |
| CapRover disk headroom | healthy | Live read-only audit now shows 38 GB total, 16 GB used and 21 GB free (44%). Docker images use 9.248 GB with 7.916 GB reported reclaimable; no cleanup is required for the current Odoo work. | Keep current and previous rollback images protected and re-measure before large builds. Never run broad `docker system prune -a`. |
| Production deploy/rollback | Odoo deployed, source cutover pending | The Control Center is deployed and healthy without changing Arcigy tenant data. No metrics credential was created and no source rows were ingested because Develop/Main telemetry isolation is not ready. | After the separate kitchen task proves Develop isolation, create narrowly scoped credentials, perform two no-write scrapes, ingest Develop first, reconcile, then enable Main. |

## Preservation classification

- `preserved`: existing Arcigy editor, tenant database, projects, pricing, BOM, exports, modules and customer-specific behavior were not changed by the Odoo addon.
- `preserved`: existing Geotherm CRM, pricebook, analytics, monthly costs, Drive OAuth/mappings and attachments are retained in the reconciled canonical package.
- `intentionally extended`: isolated read-only telemetry export script and standalone Odoo operational models/dashboard.
- `decision required`: retention deletion, Windows service changes, live credentials, scheduling, database writes outside local test, deployment and load/restore drills.

## Current P0 blockers

1. Real Develop/Main base URLs are confirmed, but their public telemetry contracts are not ready: Develop `/metrics` and Main `/metrics` return SPA HTML, while Main `/ready` also returns HTML. Two reconciled scrapes therefore remain unproven.
2. Develop is not yet proven isolated from Main data/storage, so live metrics credentials and ingestion remain intentionally disabled.
3. PostgreSQL pool/query, queue, dependency and automated backup producers are not yet deployed or reconciled; the environment-safe Prometheus adapter is ready.
4. Synthetic restore proof exists, but no approved real-backup restore proves production RPO/RTO and tenant isolation.
5. The Odoo deployment is live, but a signed-in visual/console smoke and a documented rollback exercise remain unproven.

The goal is not complete while any of these P0 items lacks evidence.
