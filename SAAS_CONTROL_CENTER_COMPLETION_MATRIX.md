# SaaS Control Center completion matrix

Authoritative specification: `pasted-text-1.txt` supplied in the Codex task.

This matrix distinguishes implemented contracts from live evidence. A seeded metric without a producing source is not considered complete.

| Area | Status | Current evidence | Remaining proof or action |
|---|---|---|---|
| Raw telemetry outside Odoo | partial | Odoo addon stores bounded current values, timeseries and domain aggregates only; no raw request, trace or product-event payload model exists. | Prove approved log, trace and product analytics backends plus their retention. |
| Develop/Main isolation | deployed, source data pending | `saas.environment` only permits `develop` and `main`; current unique key contains metric, environment and scope; history/aggregate/operational keys require environment prefix. Live ORM returns both columns on all dashboards and a separate conservative freshness/completeness summary for each environment. | Send real values from both isolated environments and reconcile source rows. |
| Metric definition contract | deployed and proven in Odoo 19 | Live ORM contains exactly 351 unique definitions across 24 dashboards; required formula, dimensions, owner, thresholds, freshness, retention, numerator/denominator labels and runbook remain present. Local contract tests and the controlled module upgrade pass. | Owner/runbook review for each production metric. External observability drill-down links remain source-specific. |
| Mandatory global filters | structurally proven | Normal relation fields and cockpit filters exist for period/comparison, service, region, release, tenant, plan, tenant size, feature, endpoint, job, integration, country, acquisition, browser, OS, device, severity and currency. Browser smoke loads the cockpit and dashboard selector. | Load representative dimension data and confirm large-tenant selector/search behavior. |
| 24 dashboard catalogue | deployed, sources partial | All 23 required dashboards plus optional AI/LLM exist. Cockpit renders one selected dashboard with KPI cards, trends, previous-period comparison, Develop/Main columns, sample size, thresholds, releases, alerts, runbook and history links. The deployed asset contains the paired freshness UI and the live registry exposes all 24 dashboards. Only two truthful Odoo-native incident rows currently exist; external source completeness remains 0% because no Arcigy source rows were ingested. | Most dashboards still need producing sources and source reconciliation. A signed-in browser smoke of the deployed cockpit remains pending. |
| Recommended base/current/history models | proven in local Odoo 19 | All named base models, `saas.metric.current` and `saas.metric.timeseries` exist with idempotency and bounded validation. Module installation, registry and database-constraint tests pass. | Production-scale query and retention evidence remains required. |
| Domain aggregates | models and client adapter proven, producers missing | All 11 named aggregate/data-quality models exist with list/pivot/graph actions and allowlisted idempotent batch ingest. A no-write client covers all ten hourly/daily domain models, enforces exact UTC cadence, Develop/Main keys, scalar allowlists, required dimensions and basic count consistency. Ten adapter tests pass. | Wire actual hourly/daily producers, validate artifacts in dry-run and reconcile totals against source systems before enabling writes. |
| Operational records | deployed and transactionally proven | Alerts, incidents, postmortem actions, backups, restore tests, DR drills, load tests, sync runs and releases exist. Backup/restore/DR/load/data-quality ingest is retry-safe. Data-quality evidence has independent fail-closed complete-event and complete-metric-population declarations. External sync evidence has a dedicated idempotent method with strict complete-count, symbolic-error, backlog, Develop/Main and stale-update validation while legacy internal sync runs remain compatible. | Feed real backup, restore, DR, load, data-quality and complete sync evidence only after source reconciliation. |
| Alert workflow | proven in live Odoo 19 | Metric warning/critical status deduplicates by environment/scope; healthy resolves it. Failed backup/restore/DR and invalid data-quality runs open P1; partial DR opens P2; P0/P1 create incident/activity; closure requires root cause and postmortem action. Registry and rollback-only live tests pass. | Complete activity/mail smoke and assign production owners and notification channels. |
| Role separation | registry and live ACL tested | Eight specified groups exist. Bot ACL is read-only on all 28 allowed models and cannot create/write/delete directly; negative tests cover finance/security and engineering/cost boundaries. A rollback-only live test confirmed direct DR creation is denied while the bounded ingest method remains usable. | Inspect menus/API interactively as every human role. Create the bot API key only when an isolated source is ready for an end-to-end sync. |
| Retention | decision required | Per-metric retention exists and `retention_preview` is non-destructive. | User approval is required before any deletion cron: approve policy, backup evidence, dry run, verification and rollback. |
| Arcigy technical source | partial, live URLs confirmed | Isolated `saasOdooMetricSync.ts` reads `/metrics` and `/ready`, calculates eligible HTTP count/success/5xx/429/in-flight/RPS/p50/p95/p99, worker uptime, PostgreSQL readiness latency and privacy-safe browser project-open/app-data-load/runtime metrics, then posts both environments through Odoo JSON-2. Live CapRover inventory confirms Develop `https://arcigy-kitchen-develop.178.104.175.242.sslip.io`, Main `https://app.arcigy.cloud`, and Odoo `https://geotherm-odoo.178.104.175.242.sslip.io`. The collector now rejects HTML/empty/unrecognized telemetry instead of emitting misleading zeros. | Develop `/metrics` currently returns SPA HTML; Main `/ready` and `/metrics` return SPA HTML. Expose authenticated Prometheus telemetry and valid readiness for both, configure secrets, then verify two no-write scrapes and reconciliation before scheduling. |
| DB/cache/queue/dependency sources | adapter ready, producers missing | Definitions, current fields and aggregate targets exist. The standalone Prometheus adapter queries Develop/Main independently, enforces an explicit environment marker on shared backends, accepts exactly one aggregate sample and has a no-write dry-run mode. Six Prometheus adapter regression tests pass. | Deploy/verify the actual PostgreSQL/pool, queue, cache and provider exporters, replace example PromQL with proven queries, then reconcile two dry runs. Do not synthesize values in Odoo. |
| Product-event pipeline | contract, rollup and current-KPI bridge ready; producer missing | Product metric definitions, `saas.product.daily` and a strict daily aggregate adapter exist. A standalone no-write rollup validates the full versioned event envelope, explicit UTC, SHA-256 identities, deduplication, Develop/Main isolation, server-only billing/auth events and bounded non-PII properties. Odoo derives sent, received, processed, rejected, loss, duplicate, schema, missing-field, late, unknown-tenant, clock-skew, processing-lag-p95 and dead-letter KPI only from a closed `event_stream_complete=true` artifact with a full consistent scalar contract. Zero-denominator rates are omitted. A rollback-only live test proved all 13 Develop event KPI and 11 defined Main zero-volume event KPI as part of the combined quality proof, then retained no synthetic rows. | Add the event producer/warehouse outside Odoo without colliding with kitchen_app, validate a real export and reconcile source totals before any write. Define authoritative cohort/eligibility inputs before activation, retention, adoption or time-to-value are emitted. |
| Metric-quality evidence | deployed contract and current-KPI bridge; producer missing | A separate `metric_quality_contract_complete=true` declaration requires an explicit eligible population plus fresh, complete, unique, valid and consistent counts, signed reconciliation difference, outliers, unexpected zeros, volume spikes, numerator-over-denominator violations, invalid negatives and missing dimensions. Every result count is non-negative and bounded by the eligible population. Five ratios are omitted when eligible is zero while seven direct evidence values remain truthful. The live rollback-only test proved 15 new Develop event/quality values, 10 defined Main zero-population values, strict rejection, idempotency and complete cleanup. | Build the authoritative scanner outside Odoo, publish only bounded aggregates, reconcile Develop first and enable writes only after source ownership and thresholds are approved. |
| Revenue/billing/CRM/support/marketing sources | target clients and reconciliation compiler ready, authoritative producers missing | Definitions plus tenant/product/cost aggregate targets and strict idempotent import are available. A no-write compiler covers all seven specified reconciliation pairs and emits only signed differences with valid/warning/invalid status. A separate bridge covers exactly 193 seeded product, tenant, revenue, marketing, support, FinOps, privacy, release, AI-change and engineering-quality KPI, requires closed UTC periods and bounded dimension scopes, and rejects raw customer payloads, unknown metrics, credentials and cross-environment keys. Eight bridge tests plus six reconciliation tests pass. | Add signed source producers from authoritative billing, CRM, support and marketing systems; approve owners/tolerances, dry-run real totals and reconcile before any Odoo write. |
| AI-assisted change risk | aggregate compiler ready, authoritative producers missing | Thirty specified metrics now cover AI-assisted change size and scope, sensitive-file classes, migrations, dependencies, tests, human/security review, post-release incidents, rollback/hotfix/reopen rates, escaped defects and regressions, repair time and LOW/MEDIUM/HIGH/CRITICAL REVIEW REQUIRED counts. The fail-closed compiler requires complete closed-period source contracts, preserves Develop/Main keys, represents missing denominators as unavailable rather than zero, and rejects code, prompts, authors, identities, file paths, secrets and raw production data. | Establish authoritative aggregate producers and the approved risk scoring policy in the separate Arcigy delivery workflow; reconcile daily and monthly dry runs before creating an Odoo bot key or writing either environment. |
| Release outcome and DORA evidence | aggregate compiler ready, authoritative producers missing | A closed-day release compiler covers 16 deployment, queue, change-failure, incident, rollback, hotfix, recovery, canary, artifact and drift metrics. It requires one consistent deployment population, exact ratio numerators/denominators, explicit rollback attempts, bounded recovery samples and honest unavailable states. It rejects raw workflow logs, commits, actors and deployment identifiers. | Connect the authoritative deployment/incident/rollback registry after the parallel release workflow is stable; reconcile Develop first and never infer change failure or rollback from failed GitHub workflows. |
| Engineering quality evidence | deployed contract, authoritative producers missing | The daily fail-closed compiler owns 57 metrics across six exact contracts: pull requests, branches, CI, tests, feature flags and architecture. It requires closed UTC periods, complete source declarations, exact ratios and consistent populations; empty sample populations are unavailable rather than false zeroes. It rejects raw logs, code/file paths, identities, credentials and cross-environment ambiguity. The live rollback-only proof created 57 isolated Develop and 57 Main rows, updated all 57 Develop rows idempotently, rejected raw/unknown payloads and direct bot writes, then explicitly rolled back. Independent reads remained `current=2`, `history=8`, `sync=0` with no temporary user. | Build and approve authoritative aggregate producers in the separate Arcigy delivery workflow. Run two reconciled no-write exports per environment before creating credentials or enabling any schedule. |
| Security/privacy/CI/CD sources | GitHub, runtime-security and privacy clients ready; authoritative producers missing | Definitions, access boundaries and security aggregate exist. A read-only GitHub adapter separately measures Develop/Main workflow metadata and repository-wide security posture. A second no-write rollup derives only anonymous daily runtime security counts from the versioned server event contract and requires an explicitly complete stream before emitting zeros. A privacy compiler now covers all 11 seeded PII, DSR, retention, consent and governance KPI; it accepts only complete global aggregates from the matching source contract and never accepts people, tenant IDs, raw requests or audit records. | Store least-privilege source credentials in the approved runner, then connect Odoo only after bot-key approval. Add the runtime event producer and real privacy/DSR/retention authoritative sources, dry-run each closed period and reconcile before any write. |
| Backup/restore/DR live evidence | deployed contract, real proof pending | Complete backup evidence now covers duration, size, 24-hour failures, snapshot count, PITR enablement/window, applicable WAL/archive health, secondary-copy health, encryption and monthly storage cost. Complete restore evidence covers duration, checksum, missing records, app smoke, tenant isolation, owner and next test while preserving explicit RPO/RTO. The new DR model covers drill age/success, failover, failback, DNS propagation, unavailable dependencies, runbook accuracy, open remediation actions, owner and next drill. The strict JSON-2 adapter supports all three with Develop/Main-prefixed idempotency keys and no-write validation; 14 focused adapter tests and the live rollback proof pass. The existing Arcigy audit remains synthetic rather than a production restore. | After an approved real-backup restore and DR drill, validate artifacts in dry-run and ingest Develop first. Add automated encrypted off-host backups and WAL/PITR where required; never upload synthetic examples as proof. |
| Odoo-native operational freshness | deployed and transactionally proven | The live 5-minute cron evaluates 76 Odoo-native metric specs per environment. Besides the earlier incident, event, quality, sync and capacity bridge, it derives 25 new Backup/Restore/DR KPI only from complete contracts; WAL health is truthfully omitted when PITR is not applicable. The image-29 rollback-only proof created six isolated Develop/Main records, proved one idempotent update, refreshed 61 truthful rows, omitted 91 unavailable rows, emitted all 25 new Develop values and the 24 applicable Main values, rejected three invalid contracts and rolled back. Independent reads proved zero synthetic Backup/Restore/DR/current/history residue beyond the original incident baseline. | Feed only approved real evidence after Develop reconciliation; the live production baseline intentionally contains no restore/DR/load, event-stream, complete metric-quality or complete external sync proof yet. |
| Load and failure evidence | deployed contract, real test missing | The live load-test model and safe ingest require closed evidence; capacity claims additionally require `representative=true`, a bounded architecture version and positive concurrency. p99/p95 and error-rate consistency are fail-closed. The rollback-only ORM smoke proved the capacity derivation without persisting its synthetic record. | Run only in an approved isolated environment with synthetic or authorized data and stated thresholds; ingest Develop first after reconciliation. |
| Odoo 19 install/tests/browser | deployed, signed-in browser pending | Image 30 runs `arcigy_saas_control_center` 19.0.1.9.0 with 24 dashboards, 351 unique definitions and an active 5-minute internal cron. The full local Node suite passes 104/104; Python/XML/CSV validation and live registry/ORM proof pass. Existing Geotherm monthly-cost and pricebook records remain readable. Login returns 200, protected health returns 401 without a key, and post-upgrade logs contain zero error or legacy-registry markers. Earlier in-app and Chrome login pages produced zero browser warning/error logs, but neither session was authenticated. | Repeat the visual and console smoke in a signed-in live Odoo session. |
| Canonical/deploy reconciliation | deployed | Feature commit `8f583b8eb6469ff421768ca5c040a611c112d274` was deployed by successful GitHub run `29537188025`. Image 30 is healthy with one app replica and one database replica; the temporary module-update environment key was removed while all 14 prior keys were preserved and `ODOO_INIT` remains `0`. | Preserve image 30 and image 29 as rollback evidence before the next module upgrade. |
| CapRover disk headroom | healthy | Live read-only audit now shows 38 GB total, 16 GB used and 21 GB free (44%). Docker images use 9.248 GB with 7.916 GB reported reclaimable; no cleanup is required for the current Odoo work. | Keep current and previous rollback images protected and re-measure before large builds. Never run broad `docker system prune -a`. |
| Production deploy/rollback | Odoo deployed, source cutover pending | Before image 30, backup `geotherm-odoo-20260716T213853Z` was structurally verified on-host and copied off-host. The DB/filestore raw archive SHA-256 is `741c4fb40319832bd3d8f8e64d00e24f52c0c2f5b29156d07a4ce637d6665086`; its AES-256-CBC/PBKDF2-600000 encrypted artifact SHA-256 is `27c6bbec2a272b809188a6db547652f01875989318867ebd2bf430a6a63d2559`. A separate mode-0600 service/env definition archive has raw SHA-256 `948e5e65222ec4e55a88e630d89759540ed072dc2b4dea00ffbd8382ea9705a2` and encrypted SHA-256 `9414ee99b480ee2717f2cfb517baeeeef9ec44049d910b34a53a59b27209e17c`. Both decrypt roundtrips passed with DPAPI-protected keys and raw local copies were removed. No metric credential or schedule was created; production rows remain at the prior Odoo-native baseline. | After the separate kitchen task proves Develop isolation, create narrowly scoped credentials, perform two no-write scrapes, ingest Develop first, reconcile, then enable Main. |

## Preservation classification

- `preserved`: existing Arcigy editor, tenant database, projects, pricing, BOM, exports, modules and customer-specific behavior were not changed by the Odoo addon.
- `preserved`: existing Geotherm CRM, pricebook, analytics, monthly costs, Drive OAuth/mappings and attachments are retained in the reconciled canonical package.
- `intentionally extended`: isolated no-write evidence compilers, 351 Odoo metric definitions and standalone operational models/dashboard.
- `decision required`: retention deletion, Windows service changes, live credentials, scheduling, database writes outside local test, deployment and load/restore drills.

## Current P0 blockers

1. Real Develop/Main base URLs are confirmed, but their public telemetry contracts are not ready: Develop `/metrics` and Main `/metrics` return SPA HTML, while Main `/ready` also returns HTML. Two reconciled scrapes therefore remain unproven.
2. Develop is not yet proven isolated from Main data/storage, so live metrics credentials and ingestion remain intentionally disabled.
3. PostgreSQL pool/query, queue, dependency and automated backup producers are not yet deployed or reconciled; the environment-safe Prometheus adapter is ready.
4. Synthetic restore proof exists, but no approved real-backup restore proves production RPO/RTO and tenant isolation.
5. The Odoo deployment is live, but a signed-in visual/console smoke and a documented rollback exercise remain unproven.

The goal is not complete while any of these P0 items lacks evidence.
